- Add "spindle" dojo layer
  - move base functionality (baseUrl, etc) into spindle layer
  - create "themes" submodule
    - site theme
  - Add ability to update page title (hook this in pastebin module)
  - load only spindle layer by default
  - load paste layer *additionally* for pastebin stuff
    - separate CSS
- Update layout
  - By default, show nothing in status bar
  - Paste module should update statusbar
    - via js?
- Import bugapp into Spindle module
- Paste application changes
  - Update views to pull from cache when available, or write to it when not
  - Add JS detection
    - If available, redirect to JS-enabled version
    - Add detection in active-grid action to use Zend_Paginator if JS is
      disabled
  - Plugin system for Paste model
    - allow registering plugins statically and/or per instance
    - surrounding add() call
      - Allowing such things as: 
        - sending updates to an IRC channel
        - sending updates to Growl or growl-like notifiers
        - triggering caching
        - triggering indexing
